# Trade quote previews

Preview clones for the HVAC Metro Detroit trade quote widget.

**Status:** PREVIEW ONLY — not live shop DNS, not a print run.

Each shop folder is a template lookalike (logo, colors, phone, signed price table services) with a “tell us what’s wrong” box. Ranges come only from the signed HVAC Metro Detroit v1 table (2026-09-20).

Do not treat these URLs as the shop’s real site.
